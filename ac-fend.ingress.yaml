apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ac-fend
spec:
  rules:
  - host: associable.fr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-app
            port:
              number: 8080